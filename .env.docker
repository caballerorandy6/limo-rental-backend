# ====================================
# Docker Development Environment
# ====================================
# Use este archivo cuando ejecutes con Docker Compose
# El backend se conectará a PostgreSQL dentro del contenedor

# Database (PostgreSQL en Docker)
# Acceso desde el backend (dentro de Docker): postgres:5432
# Acceso desde tu Mac (DBeaver): localhost:5433
POSTGRES_PRISMA_URL="postgresql://postgres:postgres@postgres:5432/limo-rental-store"

# Server Configuration
PORT=5001
NODE_ENV=development

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:3000

# ====================================
# Notas:
# ====================================
# 1. PostgreSQL Docker usa puerto 5434 en tu Mac (no interfiere con tu PostgreSQL local)
# 2. Desde el backend en Docker, se conecta al contenedor "postgres" en el puerto 5432
# 3. Tu PostgreSQL local (localhost:5432) sigue funcionando normalmente
# 4. Para conectar DBeaver a PostgreSQL Docker:
#    - Host: localhost
#    - Port: 5434
#    - Database: limo-rental-store
#    - Username: postgres
#    - Password: postgres
